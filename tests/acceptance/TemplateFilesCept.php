These are the files that are created during the plugin build process:

aiplugin{post_id}_spec.md

agent_task_{task_id}.json

codeception.yml

aiplugin{post_id}.code-workspace

aiplugin{post_id}.details.json

composer.json

linked_post_id_{post_id}.json

roo-bridge-debug.log

spec.md

task_completion_status.json